CREATE TABLE quiz_result (
    quizId     INT     NOT NULL,
    attemptId  INT     NOT NULL,
    studentId  INTEGER NOT NULL,
    finalScore DOUBLE  NOT NULL,
    PRIMARY KEY (
        quizId,
        attemptId,
        studentId
    )
);
